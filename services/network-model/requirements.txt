# services/network-model -- modelado hidraulico real (Track B, Sprint B3)
#
# wntr FIJO en 1.2.0: es la ULTIMA version con wheel manylinux para
# Python 3.9 (wntr>=1.3 requiere Python>=3.10; el toolchain de Nuitka del
# portafolio esta fijo en 3.9, ver feedback_no_source_on_server_nuitka).
# Subir esta version implica subir Python en TODO el toolchain de Nuitka
# primero -- no un bump aislado de este servicio.

wntr==1.2.0
psycopg[binary]>=3.2,<4
